#include "Simulator.h"

Simulator::Simulator(int col, int row, int turns, int nons, int preds)
	: columns{ col }, rows{ row }, maxTurns{ turns } {
	for (int i = 0; i < nons; ++i) {

	}
	for (int i = 0; i < preds; ++i) {

	}
}